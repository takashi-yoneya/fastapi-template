{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI Sample",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--host", "0.0.0.0", "--port", "81", "--reload", "--log-config", "./app/logger_config.yaml"],
      "jinja": true
    }
  ]
}
